import React from 'react';
import {CustomWebView} from './common';

const MyWebView = () => {
    return(
      <CustomWebView uri={'https://github.com/facebook/react-native'} />
    );
};
export default MyWebView;
